import numpy as np


def calc_subset_sum(weights, T, increment): 
    num_t_vals = T * (1 / increment) + 1
    include = [[0] * int(num_t_vals)] * len(weights)
    exclude = [[0] * int(num_t_vals)] * len(weights)




    return -42
    
    



